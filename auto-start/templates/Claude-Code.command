#!/bin/bash
# Claude Code Complete Startup
# This ensures Docker and MCP services are running

echo "🚀 Starting Claude Code with all services..."
bash __INSTALL_PATH__/auto-start/scripts/claude-complete-startup.sh